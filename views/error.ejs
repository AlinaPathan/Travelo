<!-- <% layout("/layouts/boilerplate") %>

<div class="row mt-3">
  <div class="alert alert-danger col-6 offset-3" role="alert">
    <h4 class="alert-heading">Error:</h4>
    <p><%= err.message %></p>  <!-- This displays the main error message -->
    <% if (err.stack) { %> <!-- Check if stack trace is available -->
      <pre><%= err.stack %></pre> <!-- This will display the stack trace -->
    <% } %>
  </div>
</div> -->
<% layout("/layouts/boilerplate") %>
<div class="row mt-3">
  <div class="alert alert-danger col-6 offset-3" role="alert">
    <h4 class="alert-heading"><%= err %></h4> <!-- Make sure you're printing the error here -->
    <p><%= err.stack %></p>  
  </div>
</div>
